export const PAGINATION_LIMIT: number = 12

export enum Slice {
  Favorites = 'favorites'
}

export enum FilterStock {
  Available = 'available'
}

export enum Sorting {
  Newest = 'newest',
  Lowest = 'lowest',
  Highest = 'highest'
}
